# Success Criterion 2.1.2 - No Keyboard Trap

On Android, always add a close button to overlays and popups.

<pre>
    <code class="language-kotlin">
val builder = <a href="https://developer.android.com/reference/android/app/AlertDialog.Builder">AlertDialog.Builder</a>(this)

builder.<a href="https://developer.android.com/reference/android/app/AlertDialog.Builder#setNegativeButton(java.lang.CharSequence,%20android.content.DialogInterface.OnClickListener)">setNegativeButton</a>("Close") { dialog, which ->
  // Close
}        
    </code>
</pre>

```kotlin
val builder = AlertDialog.Builder(this)

builder.setNegativeButton("Close") { dialog, which ->
  // Close
}
```
