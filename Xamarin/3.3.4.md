```c#
    var accept = await App.Current.MainPage.DisplayAlert("Confirm Appt membership", "Your bank account will be billed.", "Proceed", "Cancel");

    if (accept) {
        // the user accepted the alert
    } else {
        // the user cancelled the alert
    }
```
